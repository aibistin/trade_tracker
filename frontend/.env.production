# Production build environment (picked up automatically by `pnpm build`)
# The built dist/ is served by the trade_tracker_front systemd service via `pnpm run preview`.
# Dev frontend (pnpm dev) ignores this file and uses the default in src/config.js (port 5000).
VITE_API_BASE_URL=http://localhost:3000/api
